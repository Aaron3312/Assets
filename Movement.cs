using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement : MonoBehaviour
{

    //ciclo de vida de un script
    //1. Awake -corre al inicio una solaa vez se ejecuta sin importar si el script esta activo o no
    //2. Start - corre al inicio una sola vez y despues de awake se ejecuta solo si el script esta activo
    //3. Update - is called once per frame
    //4. FixedUpdate
    //5. LateUpdate
    //6. OnDisable
    //7. OnDestroy

    //variables
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
